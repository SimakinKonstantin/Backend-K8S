apiVersion: v1
kind: Service
metadata:
  name: kafka
spec:
  selector:
    pod: kafka-pod # Трафик будет перенаправляться к подам с этой меткой.
  ports:
    - port: 9092 # Порт, куда будет перенаправляться трафик
      name: kafka-port-1
    - port: 9093
      name: kafka-port-2
  type: ClusterIP