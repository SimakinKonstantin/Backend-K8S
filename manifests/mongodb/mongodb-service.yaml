apiVersion: v1
kind: Service
metadata:
  name: mongodb
spec:
  selector:
    pod: mongo-pod # Трафик будет перенаправляться к подам с этой меткой.
  ports:
    - port: 27017 # Порт, куда будет перенаправляться трафик
  type: ClusterIP