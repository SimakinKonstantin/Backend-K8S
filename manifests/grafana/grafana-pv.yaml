apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain # Чтобы данные сохранились после удаления PVC.
  hostPath:
    path: /mnt/data/grafana # Путь на ноде, где будут лежать данные.
  claimRef:
    namespace: default
    name: grafana-pv-claim
